# CampusRide — ROADMAP.md

**This file is the project's authoritative state file.**

## Working agreement

1. Read this ROADMAP.md at the start of every session.
2. Build ONLY the current phase; if work pulls toward a later phase, STOP and flag it instead of building it.
3. End every phase by updating this file (status + what was built) AND with the phase's exit-gate checklist; wait for human verification before starting the next phase.
4. Money is always integer pesewas. The app name is always the one `APP_NAME` constant in `apps/server/src/config.ts` (currently `"CampusRide"`), never hardcoded elsewhere.
5. Keep external integrations behind their service interfaces (swappable: `PaymentService`, `RouteService`, `OtpService`, each with a `Dummy*` implementation wired in `apps/server/src/index.ts`).
6. A prior confused side-session nearly rebuilt Phase 2 dispatch from scratch (a stray `broadcast.ts` + a `TIMEOUT` cancel reason). **If you ever find those, they are wrong** — the 90s dispatch timeout lives entirely in `processTimeouts` (`apps/server/src/services/ride/timeouts.ts`). Do not recreate them.

---

## 1. Locked decisions (do not revisit without explicit human instruction)

- **Product:** Campus ride-hailing for University of Ghana, Legon. Web v1 paused; this is a clean rebuild. Working name **"CampusRide"**, defined once as `APP_NAME` in [apps/server/src/config.ts](apps/server/src/config.ts).
- **Stack:** TypeScript everywhere, strict mode, no `any`. npm-workspaces monorepo: `packages/shared`, `apps/server`, `apps/mobile`. Backend: Fastify (`apps/server/src/index.ts`). DB: PostgreSQL on Neon via Prisma (`DATABASE_URL` pooled + `DIRECT_URL` direct, see `apps/server/prisma/schema.prisma`). Mobile: Expo + Expo Router (SDK 51, deps via `expo install`, no `legacy-peer-deps`) — currently just `apps/mobile/app/_layout.tsx`, `apps/mobile/app/index.tsx`, `apps/mobile/lib/api.ts` (health-check screen only). Real-time: Socket.io, attached to the Fastify HTTP server in `apps/server/src/index.ts` (currently only logs connect/disconnect — no app events emitted yet). Payments: Paystack/Moolre planned for Phase 4 — `PaymentService` interface + `DummyPaymentService` exist now (`apps/server/src/services/payment/`). Maps: Mapbox display + Socket.io driver dot + dummy ETA planned, behind `RouteService`/`DummyRouteService` (`apps/server/src/services/route/`). OTP: mNotify planned — `OtpService`/`DummyOtpService` exist now (`apps/server/src/services/otp/`). Photos: Cloudinary (not yet integrated).
- **Money:** integer pesewas everywhere (GHS 1 = 100 pesewas), never floats. Defined in [packages/shared/src/pricing/pricing.ts](packages/shared/src/pricing/pricing.ts). Invariant `commission + driverShare === totalFare` holds exactly (`splitFare`, `COMMISSION_PCT: 15` / 85% to driver).
- **Ride model (TWO types)** — `RideType` enum (`LONE` | `SHARED`) in schema:
  - **LONE**: flat `LONE_FARE: 1500`, exactly 1 rider (`validateLoneOccupancy`), immediate.
  - **SHARED**: dynamic by final occupancy at departure, max `MAX_SHARED_OCCUPANCY: 4`. Per-rider rate via `SHARED_FARES = { 1: 1000, 2: 700, 3: 600, 4: 500 }` (`getSharedFarePerRider`); total per car = occupancy × rate, i.e. **1000 / 1400 / 1800 / 2000** as occupancy goes 1→4 (`getSharedTotals`). Commission 15% platform / 85% driver on that total. Price locks per-passenger on join via `recomputeLockedFares` ([apps/server/src/services/ride/lockedFare.ts](apps/server/src/services/ride/lockedFare.ts)) — **downward-only** (a join can lower everyone's locked fare as occupancy grows, but a later cancellation never raises it back). Driver absorbs empty-seat risk; platform never subsidizes. Charged at departure. Riders can cancel any time before departure.
- **Dispatch:** broadcast to eligible drivers (online, approved, in pickup zone or an adjacent zone, no active ride) via `getEligibleDrivers` ([apps/server/src/services/ride/dispatch.ts](apps/server/src/services/ride/dispatch.ts)); first to claim wins via `claimRide`'s atomic conditional `updateMany` (`WHERE status='REQUESTED' AND driverId IS NULL`). 90s no-claim → `AWAITING_RIDER_DECISION` → rider picks `KEEP_WAITING` / `SWITCH_TO_LONE` (only if exactly 1 active passenger) / `CANCEL` via `riderDecision` ([apps/server/src/services/ride/riderDecision.ts](apps/server/src/services/ride/riderDecision.ts)); a further 90s grace with no decision → `CANCELLED` / `NO_DRIVERS_AVAILABLE`. **Both 90s timeouts are owned entirely by `processTimeouts`** (`shouldTimeout`, `shouldExpireDecision` in [apps/server/src/services/ride/timeouts.ts](apps/server/src/services/ride/timeouts.ts)), polled every 15s from `bootstrap()` in `apps/server/src/index.ts`. **There is intentionally NO `broadcast.ts` and NO `TIMEOUT` cancel-reason** — `RideCancelReason` is exactly `RIDER_CANCELLED | DRIVER_BACKED_OUT | NO_DRIVERS_AVAILABLE | ALL_PASSENGERS_LEFT | MERGED_INTO_ANOTHER_RIDE`.
- **Best-fit (Phase 2e):** proximity-dominant scoring — `BEST_FIT = { PROXIMITY_SAME_ZONE: 10, PROXIMITY_ADJACENT: 5, PROXIMITY_TWO_HOP: 1, PROXIMITY_FAR: 0, ROUTE_ALIGNMENT_BONUS: 1 }` ([packages/shared/src/ranking/bestFit.ts](packages/shared/src/ranking/bestFit.ts)). The +1 route-alignment bonus (driver's zone on the pickup→dropoff adjacency path) can only break ties between equally-proximate drivers — spacing guarantees it never lets a farther driver outrank a closer one. Ties share `bestFit: true`; ≥1 driver is always `bestFit: true` if any eligible drivers exist. This is a driver-side-only scoring concept (`designateBestFit`) — it does **not** broadcast anything; wiring the `bestFit` flag into a Socket.io event and the driver UI is deferred to Phase 5.
- **Competitive guarantees (encoded as tests in `packages/shared/src/pricing/pricing.test.ts`):** no shared rider ever pays more than GHS 10 (1000 pesewas, the 1-occupancy rate); a lone-shared driver's car total is ≥ 1000; a full (4-seat) car charges 500/rider.

---

## 2. Phase status

| Phase | Status | Summary |
|---|---|---|
| 0 — Foundation | ✅ COMPLETE | npm-workspaces monorepo; `@rida/shared` types; Fastify server; Neon+Prisma; full `schema.prisma`; swappable service stubs (`PaymentService`/`RouteService`/`OtpService` + Dummy impls); Socket.io attached in `bootstrap()`; Expo health-check screen; 15 zones seeded (`apps/server/prisma/seed.ts`, idempotent). |
| 1 — Pricing engine | ✅ COMPLETE | Pure functions in `packages/shared/src/pricing/pricing.ts`: `validateLoneOccupancy`, `validateSharedOccupancy`, `splitFare`, `getSharedFarePerRider`, `getSharedTotals`, etc. Flat two-type model, exact-pesewa commission split, 51 competitive-guarantee tests. |
| 2a — Lifecycle + state machine | ✅ COMPLETE | `RIDE_TRANSITIONS`/`transitionRide`, `PASSENGER_TRANSITIONS`/`transitionPassenger`, `isActivePassengerStatus` ([stateMachine.ts](apps/server/src/services/ride/stateMachine.ts)); `applyRideTransition`/`applyPassengerTransition`/`joinSharedRide(Tx)` ([rideService.ts](apps/server/src/services/ride/rideService.ts)); downward-only `recomputeLockedFares` ([lockedFare.ts](apps/server/src/services/ride/lockedFare.ts)); all transactional with injectable `now`. |
| 2b — Broadcast + atomic claim + timeout | ✅ COMPLETE | `getEligibleDrivers`, `claimRide` (atomic conditional update, throws `RideAlreadyClaimedError` on loss), `notifyEligibleDrivers`/`broadcastRide` (Socket.io TODO no-ops, deferred to Phase 5) — all in [dispatch.ts](apps/server/src/services/ride/dispatch.ts). `shouldTimeout`/`shouldExpireDecision`/`processTimeouts` in [timeouts.ts](apps/server/src/services/ride/timeouts.ts), polled every 15s via `setInterval` in `bootstrap()`. `riderDecision` (KEEP_WAITING/SWITCH_TO_LONE/CANCEL) in [riderDecision.ts](apps/server/src/services/ride/riderDecision.ts). Migration `20260607150000_phase2b_dispatch_timeout` added `RideStatus.AWAITING_RIDER_DECISION`, `Ride.broadcastStartedAt`/`decisionStartedAt`. |
| 2c — Ranking + combinable surfacing | ✅ COMPLETE | `RANKING`, `areCombinable`, `scoreRequest` in [packages/shared/src/ranking/ranking.ts](packages/shared/src/ranking/ranking.ts) (exported via shared barrel). Server-side `rankIsolatedRequests`, `detectClusters` (union-find), `suggestFillsForRide` in [apps/server/src/services/ride/ranking.ts](apps/server/src/services/ride/ranking.ts). Read-only, anti-starvation wait boost, no DB writes. |
| 2d — Shared-car assembly | ✅ COMPLETE | `addRiderToCar` (atomic absorb of a pending request into a claimed car; merge represented as the absorbed ride going `CANCELLED` / `MERGED_INTO_ANOTHER_RIDE` with `mergedIntoRideId` pointing at the anchor, rider moved onto the anchor via `joinSharedRideTx`) and `departRide` (ARRIVED → IN_PROGRESS, freezes locked fares) — both in [assembly.ts](apps/server/src/services/ride/assembly.ts). New errors: `NotRideOwnerError`, `RideNotFillableError`, `NoSeatsAvailableError`, `RidesNotCombinableError`, `RequestRideUnavailableError`, `RideNotReadyToDepartError` ([errors.ts](apps/server/src/services/ride/errors.ts)). Migration `20260607190527_phase2d_shared_car_assembly` added `RideCancelReason.MERGED_INTO_ANOTHER_RIDE` and `Ride.mergedIntoRideId` (self-relation FK, `onDelete: SetNull`, indexed). |
| 2e — Best-fit designation | ✅ COMPLETE (this session) | New shared module [packages/shared/src/ranking/bestFit.ts](packages/shared/src/ranking/bestFit.ts): `BEST_FIT` constants, graph helpers `areAdjacent`/`hopDistance`/`isOnPath`, and `designateBestFit(request, eligibleDrivers, adjacency)` — pure, read-only, no DB/Socket/UI. Re-exported from [apps/server/src/services/ride/ranking.ts](apps/server/src/services/ride/ranking.ts) alongside the existing `RANKING`/`areCombinable`/`scoreRequest` re-exports. 21 new tests in `packages/shared/src/ranking/bestFit.test.ts`, including the proximity-dominance property. No `broadcast.ts`, no `TIMEOUT` enum value, no `processTimeouts` change, no Socket/UI/payment code. |
| 3 — Auth + OTP | ✅ COMPLETE (this session) | Phone+OTP auth, no passwords. Schema: `OtpPurpose` enum, `OtpCode`/`RefreshToken` models, `Driver` profile fields (`carMake`/`carModel`/`carColor`/`plate`/`photoUrl`) made nullable for pending drivers (migration `20260607215545_phase3_auth_otp`). Auth service in [apps/server/src/services/auth/](apps/server/src/services/auth/): `requestOtp`/`verifyOtp` ([otp.ts](apps/server/src/services/auth/otp.ts), rate limits `OTP_MAX_SENDS_PER_15MIN: 3` / `OTP_MAX_SENDS_PER_HOUR: 5`, `OTP_MAX_VERIFY_ATTEMPTS: 5`, `OTP_TTL_MINUTES: 5`, `OTP_LENGTH: 6` in [constants.ts](apps/server/src/services/auth/constants.ts)); `signup`/`login`/`refresh`/`logout`/`revokeAllForUser`/`completeDriverProfile` in [authService.ts](apps/server/src/services/auth/authService.ts). Both OTP codes and refresh tokens are stored as sha256 hashes ([hash.ts](apps/server/src/services/auth/hash.ts)) — OTPs are short-lived/single-use/rate-limited so sha256 is adequate without a native bcrypt dep; refresh tokens are 256-bit random values, the standard case for fast hashing. Two-step verification: `/auth/verify-otp` consumes the OTP and returns a short-lived signed `verifiedToken` JWT (`VERIFICATION_TOKEN_TTL_MINUTES: 5`, [tokens.ts](apps/server/src/services/auth/tokens.ts)) that `/auth/signup`/`/auth/login` then verify — the raw OTP is never re-presented. Access tokens (`ACCESS_TOKEN_TTL_MINUTES: 30`, payload `{userId, role}`) signed with `JWT_SECRET`; refresh tokens (`REFRESH_TOKEN_TTL_DAYS: 30`) are rotated on every `/auth/refresh` (old one revoked, reuse rejected). Routes in [apps/server/src/routes/auth.ts](apps/server/src/routes/auth.ts): `POST /auth/{request-otp,verify-otp,signup,login,refresh,logout}`, authed `GET /me` and `POST /driver/profile` (driver-only, fills the pending `Driver` row created at DRIVER signup — `photoUrl` accepts a placeholder URL pending Cloudinary). `requireAuth` preHandler in [apps/server/src/middleware/auth.ts](apps/server/src/middleware/auth.ts) validates the access token and attaches `request.user`. Active service singletons centralized in [apps/server/src/services/active.ts](apps/server/src/services/active.ts) to avoid circular imports with `index.ts`. Mobile auth flow (Expo Router) in `apps/mobile/app/auth/{phone,otp,signup}.tsx` + `apps/mobile/lib/auth/` (`AuthContext`, `tokenStore` for in-memory access token, `storage.ts` for expo-secure-store refresh token, axios interceptor in `lib/api.ts` that refreshes on 401); `app/index.tsx` is now the authed landing screen (shows `/me` data, redirects to `/auth/phone` if unauthenticated). **mNotify wiring (final step):** `MnotifyOtpService` ([apps/server/src/services/otp/MnotifyOtpService.ts](apps/server/src/services/otp/MnotifyOtpService.ts)) implements `OtpService` via mNotify's Quick SMS API with `sms_type: "otp"`; selected in `services/active.ts` only when `MNOTIFY_ENABLED=true` — tests and dev always get `DummyOtpService` (logs the code to console). Manual verification script: `apps/server/src/scripts/sendTestOtp.ts` (not run by tests/CI). 33 new tests (9 OTP unit + 12 authService unit + 12 route/middleware integration), all against `DummyOtpService`/`CapturingOtpService` — no real SMS sent. No ride/matching/payment/ranking code touched. |
| 4 — Payments | ⬜ NOT STARTED | Real `PaymentService` (Paystack/Moolre), collection + disbursement, wired to departure. **GATE: confirm disbursement API before building.** |
| 5 — Mobile rider flow | ⬜ NOT STARTED | Includes wiring real Socket.io ride-broadcast events (currently TODO no-ops in `dispatch.ts`) and the `bestFit` flag from `designateBestFit`, plus the two-section ("Best for you" / "Other requests") driver request UI — deferred here from the 2e best-fit work. |
| 6 — Mobile driver flow | ⬜ NOT STARTED | |
| 7 — Admin + polish + deploy | ⬜ NOT STARTED | |

### Current verified test counts (this session)
- `packages/shared`: **81/81 pass** (51 pricing + 9 ranking + 21 bestFit), `tsc --noEmit` clean.
- `apps/server`: **129/129 pass** (96 prior + 33 new Phase 3 auth: 9 OTP + 12 authService + 12 routes/middleware), `tsc --noEmit` clean.
- `apps/mobile`: `tsc --noEmit` clean (auth screens + AuthContext + axios interceptor added).

---

## 3. Known issues to fix BEFORE real traffic (not blocking build)

- **Neon connection-pool capacity under concurrency.** Repeatedly forced raising Prisma transaction options to work around connection contention: 2a's `applyRideTransition`/`joinSharedRide`/etc. use `TX_OPTIONS = { timeout: 20000 }`; 2b needed a Neon-cold-start warm-up (`apps/server/src/test/setup.ts`) plus retries around `P1001`; 2d's `addRiderToCar` needed `maxWait: 10000` added to its `TX_OPTIONS` to fix a `P2028` ("unable to start a transaction in the given time") under concurrent absorbs. The race-arbitration logic itself is correct (atomic conditional `updateMany` patterns work) — this is purely connection-pool capacity. Before launch: tune Neon's pooler (PgBouncer-style pool size), shorten hot-path transactions, and size the pool for expected concurrency. **Do not just keep raising client-side timeouts as load grows.**

---

## 4. Deferred (NOT in this build)

- Real paid maps provider (swap in behind `RouteService` once funded).
- Food + Courier verticals — UI "Soon" tiles only, zero backend.
- Multi-campus expansion.
- Push notifications.
- Per-car rider consent (auto-enroll for now — locked decision from Phase 2d).

---

## 5. Discrepancies found vs. the source summary used to build this file

- The source summary described the dispatch-timeout outcome as `CANCELLED/NO_DRIVERS_AVAILABLE` after the post-decision grace period — confirmed correct against `processTimeouts`/`shouldExpireDecision` in the actual code, no discrepancy.
- No other discrepancies found: `RideCancelReason` has exactly the 5 values listed (no `TIMEOUT`), no `broadcast.ts` exists anywhere in `apps/server/src/`, and `processTimeouts`/`timeouts.ts` are unmodified from their Phase 2b form.
- `apps/mobile` now has the Phase 3 auth gate (`app/auth/{phone,otp,signup}.tsx`, `app/index.tsx` as authed landing screen, `lib/auth/`) on top of the original health-check (`lib/api.ts` health query still used elsewhere) — no rider/driver flow UI exists yet, consistent with Phases 5/6 being NOT STARTED.
